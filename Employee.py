class Employee:
    def srinath(name, age, address):
        name=self.name
        age=self.age
        address=self.address
        def displaysrinath(skars):
            print(name, age, address)
            def displayskars(ob):
                ob.name=hello
                ob.age=13
                ob.address=jekke
                
                
